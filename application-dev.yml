spring:
  cloud:
    openfeign:
      client:
        config:
          product-service:
            url: http://localhost:8080
          user-service:
            url: http://localhost:8081
          default:
            connectionTimeout: 3000
            readTimeout: 5000

logging:
  level:
    #root: debug
    com.lendmate: DEBUG