spring:
  cloud:
    openfeign:
      client:
        config:
          product-service:
            url: http://localhost:8080
          user-service:
            url: http://localhost:8081

logging:
  level:
    root: debug